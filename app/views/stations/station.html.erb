Info for <%= station.identifier %>: <%= station.name %> (<%= station.address %>)
<div class="flexbox vertical stretch">
  <%= render(partial: "bikes/row", collection: Bike.where(current_station_id: station.identifier), as: :bike )%>
</div>