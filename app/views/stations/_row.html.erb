<div class="record-row flexbox vertical stretch">
  <div class="record-name">
    Station <%= station.identifier %>: <%= station.name %>
    </div>
    <div class="record-details">
        <%= station.address %>
    </div>
    <div class="record-details">
        <%= Bike.where(current_station_id: station.id).length %> available bikes
    </div>
  <div class="flexbox vertical stretch">
    <%= button_to('View Station', station_path(station), class: "mt-3 custom-btn btn-outline", method: :get) %>
  </div>
</div>